import { AppShell } from "@/components/layout/app-shell";

export default function AnalisisVentasPage() {
  return (
    <AppShell>
      <h2 className="text-2xl font-semibold text-ink">Análisis de ventas</h2>
      <p className="mt-2 text-sm text-slate-600">Base para ventas por período, vendedor, caja y sucursal.</p>
    </AppShell>
  );
}
