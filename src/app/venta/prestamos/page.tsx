import { AppShell } from "@/components/layout/app-shell";

export default function VentaPrestamosPage() {
  return (
    <AppShell>
      <h2 className="text-2xl font-semibold text-ink">Préstamos y garantías</h2>
      <p className="mt-2 text-sm text-slate-600">Base para ítems retornables y dinero en garantía.</p>
    </AppShell>
  );
}
