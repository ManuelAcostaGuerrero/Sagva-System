import { AppShell } from "@/components/layout/app-shell";

export default function VentasAbiertasPage() {
  return (
    <AppShell>
      <h2 className="text-2xl font-semibold text-ink">Ventas abiertas</h2>
      <p className="mt-2 text-sm text-slate-600">Base para múltiples ventas temporales por vendedor.</p>
    </AppShell>
  );
}
