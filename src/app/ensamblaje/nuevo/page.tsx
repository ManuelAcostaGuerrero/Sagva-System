import { AppShell } from "@/components/layout/app-shell";

export default function NuevoEnsamblajePage() {
  return (
    <AppShell>
      <h2 className="text-2xl font-semibold text-ink">Nuevo ensamblaje</h2>
      <p className="mt-2 text-sm text-slate-600">Base para producto final, componentes y cantidades requeridas.</p>
    </AppShell>
  );
}
