import { AppShell } from "@/components/layout/app-shell";

export default function CajaActivaPage() {
  return (
    <AppShell>
      <h2 className="text-2xl font-semibold text-ink">Caja activa</h2>
      <p className="mt-2 text-sm text-slate-600">Panel preparado para apertura, cierre y movimientos visibles por permiso.</p>
    </AppShell>
  );
}
