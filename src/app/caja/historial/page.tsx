import { AppShell } from "@/components/layout/app-shell";

export default function CajaHistorialPage() {
  return (
    <AppShell>
      <h2 className="text-2xl font-semibold text-ink">Historial de cierres</h2>
      <p className="mt-2 text-sm text-slate-600">Listado base para cierres, arqueos y diferencias.</p>
    </AppShell>
  );
}
