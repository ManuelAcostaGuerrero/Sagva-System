import { AppShell } from "@/components/layout/app-shell";

export default function SeguridadAuditoriaPage() {
  return (
    <AppShell>
      <h2 className="text-2xl font-semibold text-ink">Auditoría</h2>
      <p className="mt-2 text-sm text-slate-600">Base para acciones críticas, datos anteriores y datos nuevos.</p>
    </AppShell>
  );
}
